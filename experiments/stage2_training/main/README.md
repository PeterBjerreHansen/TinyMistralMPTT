# Stage 2 main comparison

This directory is reserved for the principal locked training comparison.
`LOCKED_PROTOCOL.md` is the gate. Until it says `LOCKED`, results from protocol
development remain development evidence and no main-run model YAMLs should be
promoted into `configs/stage2/`.
