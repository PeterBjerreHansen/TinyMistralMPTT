# Historical benchmarks

This directory contains useful evidence from superseded experiment protocols.
Historical studies are read-only records; new runs belong under `controls/`,
`development/`, `ad_hoc/`, `core/`, or `efficiency/` according to their role.
