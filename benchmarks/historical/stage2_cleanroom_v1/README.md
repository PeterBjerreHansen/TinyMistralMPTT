# Stage 2 clean-room results

This directory retains the result reports from the superseded 512-token
clean-room campaign. Runnable configs, generated checkpoints, and the old data
recipe were intentionally removed when the 2048-token qualification became the
active pre-lock substrate.

The reports preserve the useful learning-rate, K-schedule, pass-depth, and
inference evidence. The last pre-reorganization repository snapshot that still
contained the runnable historical lineage was:

```text
2b6302a111460b0a7f2ec54f538216231ca1cf3c
```

That commit is an archival pointer, not a claim that every historical run was
executed from exactly that source revision. New work belongs under the current
benchmark taxonomy.
