# Pre-tape efficiency evidence

Read-only engineering measurements produced before the unified `tape` architecture. They retain the historical variant labels contained in the original result JSON and are not consumed by active benchmark tooling.
