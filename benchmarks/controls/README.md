# Benchmark controls

Reusable reference runs live here. They validate the execution substrate and
provide vanilla baselines for later comparisons; they are not ad-hoc
experiments or core scientific claims.

- `substrate/`: longer vanilla controls for the local and GPU environments.
- `smoke/`: short CPU correctness runs.

Each control keeps its configuration and generated outputs under the same
directory.
