# Wiring results

Raw wiring runs are written beneath `generated/` and ignored by Git. Commit a
small result note here only when the outcome is needed to justify a later
protocol decision. Downstream configs may still initialize from the local
`generated/<arm>/latest.pt` checkpoint.
