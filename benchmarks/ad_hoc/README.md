# Ad-hoc benchmarks

Use this directory for one-off exploratory comparisons that do not define a
reusable protocol decision. Validation diagnostics such as pass-depth sweeps or
exact-vs-recurrent drift usually need no permanent benchmark directory at all;
write them to a temporary path unless the result becomes worth retaining.
