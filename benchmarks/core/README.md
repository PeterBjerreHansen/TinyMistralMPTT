# Core benchmarks

This directory is reserved for the larger, predeclared experiments that test
the project's central claims. It is intentionally empty until the development
benchmarks have established and locked the protocol.
