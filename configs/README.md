# Configurations

- `substrate/`: reusable vanilla development configs.
- `smoke/`: small local correctness runs.
- `evaluation/`: lm-evaluation-harness task suites.
- `stage2/`: locked main training configs.
- `../experiments/stage2_cleanroom_v1/configs/`: protocol arms and controls.

Generated output paths mirror the config hierarchy under `runs/`.
